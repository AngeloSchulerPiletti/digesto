import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'email.digesto.digesto',
  appName: 'Digesto',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
